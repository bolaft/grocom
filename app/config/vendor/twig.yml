twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%

services:
    twig.text_extension:
        class: Twig_Extensions_Extension_Text
        tags:
            - name: twig.extension    # Doctrine Extension listeners to handle behaviors